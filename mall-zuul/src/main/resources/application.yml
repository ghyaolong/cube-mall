eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 10500
spring:
  application:
    name: mall-zuul
zuul:
  routes:
    api-a:
      path: /api-product/**
      serviceId: product-center
    api-b:
      path: /api-order/**
      serviceId: order-center